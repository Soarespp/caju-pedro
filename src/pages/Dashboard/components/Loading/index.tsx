import * as S from "./styles";

const Loading = () => {
  return (
    <S.ContainerLoader>
      <S.logo>
        <p>loading...</p>
      </S.logo>
    </S.ContainerLoader>
  );
};

export default Loading;
